# ActionEngine (future phase)

Not implemented yet. `future/actions/service.py` sketches the intended
interface only — it is not wired into the request path.

Planned responsibilities:
- executing TOOL-type requests (function calling / device actions);
- eventually bridging to Home Assistant and other integrations.
