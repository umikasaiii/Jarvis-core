# VisionService (future phase)

Not implemented yet. `future/vision/service.py` sketches the intended
interface only — it is not wired into the request path.

Planned responsibilities: image/frame understanding requests from Android
(e.g. camera snapshots), beyond the text-only contract used today.
