# VoiceService (future phase)

Not implemented yet. `future/voice/service.py` sketches the intended
interface only — it is not wired into the request path.

Planned responsibilities: STT (speech-to-text) ingestion and TTS
(text-to-speech) output, likely as an additional streaming endpoint.
